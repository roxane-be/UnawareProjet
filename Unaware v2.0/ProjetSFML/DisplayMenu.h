#ifndef DISPLAYMENU_H
#define DISPLAYMENU_H

#include "definition.h"

//affichage du menu
void DisplayMenu(GameData*);

void DisplayCredit(GameData*);
void DisplayOption(GameData*);
void DisplayVolume(GameData*);
void DisplayControle(GameData*);

void DisplayPause(GameData*);
#endif

